#!/bin/bash
for f in "$@"; do
  [[ -f "$f" ]] || continue
  base="${f%.*}"
  mbuffer -m 2G -i "$f" | ffmpeg -hwaccel cuda -hwaccel_output_format cuda \
    -i pipe:0 \
    -c:v hevc_nvenc \
    -c:a copy \
    -b:v 0 \
    -y "${base}-opt.mp4"
done
